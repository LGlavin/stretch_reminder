<%= form_for @reminder do |f| %>
 

<% if @reminder.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@reminder.errors.count, "error") %> prohibited this event from being saved:</h2>

    <ul>
    <% @reminder.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
  <p>
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </p>
<div class="field">
  <%= f.label :days %><br/>
  <%= f.date_select :dispatch_time, :ignore_time => true %>
</div>
 
 <div class="field">
  <%= f.label :time %><br/>
  <%= f.time_select :datetime, :ignore_time => true %>
</div>

  <p><%= f.submit 'Create Reminder' %></p>
<% end %>